import Image from "next/image";
import { samplePhotos } from "@/data/meal-plans/samplePhotos";

export default function DailyMealShowcase() { return <section className="bg-white py-20"><div className="mx-auto max-w-7xl px-6 md:px-8"><div className="mx-auto mb-12 max-w-3xl text-center"><p className="mb-3 text-sm font-semibold uppercase tracking-widest text-[#C9A18A]">Daily Meal Showcase</p><h2 className="text-3xl font-bold text-[#1F4E4C] md:text-5xl">典型每日月子餐搭配</h2><p className="mt-5 leading-8 text-gray-600">早餐暖胃调养，午餐补充优质蛋白，晚餐温和恢复，搭配汤水、甜品、水果与点心。</p></div><div className="grid gap-5 md:grid-cols-2">{samplePhotos.map((photo,index)=><div key={photo.src} className={`group relative overflow-hidden rounded-[2rem] border border-[#E8DCC9] bg-[#FAF8F5] shadow-sm ${index===0 ? "md:col-span-2" : ""}`}><div className="relative aspect-[16/10]"><Image src={photo.src} alt={photo.alt} fill className="object-cover transition duration-500 group-hover:scale-105" sizes={index===0 ? "100vw" : "(max-width: 768px) 100vw, 50vw"} /></div><div className="absolute bottom-0 left-0 right-0 bg-gradient-to-t from-black/55 to-transparent p-5 text-white"><p className="font-semibold">{photo.title}</p></div></div>)}</div></div></section>; }
